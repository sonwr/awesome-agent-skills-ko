# README intro-first validate status gate

소개 아래에서 대상 사용자 -> 첫 검증 명령 -> 다음 문서가 한 화면에 남는지 먼저 확인하고, validator가 실패하면 README intro-first 변경은 커밋/푸시하지 않습니다.

English mirror: First confirm that audience -> first validation command -> next doc still stays in one screen under the intro, and do not commit/push intro-first README changes when the validator is failing.
