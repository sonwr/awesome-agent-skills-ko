# README intro-first command handoff note

README 첫 화면에서 소개 바로 아래에 **누가 시작하는가 -> 첫 검증 명령 -> 다음 문서**가 한 줄 흐름으로 남는지 빠르게 점검하는 메모입니다.

English mirror: A quick note for checking that **who starts -> first validation command -> next doc** stays visible as one uninterrupted landing-line handoff directly under the README intro.

## 빠른 점검 / Quick check

- 소개 문장 바로 아래에 대상 사용자 신호가 보입니다.
- 첫 검증 명령 `python3 templates/scripts/validate_template.py` 가 같은 시야에 남습니다.
- 다음 문서 `examples/quickstart.md` handoff가 끊기지 않습니다.

## 편집 규칙 / Editing rule

소개 문장을 줄이거나 새 링크를 추가할 때도 위 세 요소를 첫 화면 같은 시야에 유지합니다.

English mirror: Even after tightening the intro or adding links, keep those three cues visible in the same first-screen window.
