# 첫 화면 intro-first 검증 후 다음 문서 순서 메모 / Intro-first post-validation next-doc order note

README 첫 화면에서는 validator를 돌린 뒤에도 **대상 사용자 -> 첫 검증 명령 -> 다음 문서** 순서가 그대로 보여야 합니다.

The README first screen should still preserve **audience -> first validation command -> next doc** immediately after validation.
