# README intro-first start signal card

README 첫 화면에서는 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 한 카드처럼 바로 보여야 합니다. English mirror: Keep **audience -> first validation command -> next doc** visible as one starter card on the first screen.
