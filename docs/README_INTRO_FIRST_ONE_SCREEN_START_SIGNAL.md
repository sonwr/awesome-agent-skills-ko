# README intro-first 원스크린 시작 신호 / One-screen start signal

소개 바로 아래에서 `대상 사용자 -> 첫 검증 명령 -> 다음 문서`가 한 번에 보여야 합니다.

English mirror: Directly under the intro, keep `audience -> first validation command -> next doc` visible in one screen.
