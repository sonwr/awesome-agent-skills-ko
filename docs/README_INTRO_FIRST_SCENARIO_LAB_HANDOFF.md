# README intro-first 시나리오 랩 핸드오프

README 첫 화면에서는 `scenario_lab` 같은 래퍼 별칭도 빠르게 소개해, 독자가 예제 파일을 읽을 때 실제 입력 구조를 바로 추적할 수 있게 한다.

In intro-first README flows, mention wrapper aliases like `scenario_lab` early so readers can map example files to the real input structure without guessing.
