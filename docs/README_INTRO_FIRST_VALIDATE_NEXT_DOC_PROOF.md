# README intro-first validate-next-doc proof

README 첫 화면에서는 소개 바로 아래에 다음 3가지를 같은 시야에 남깁니다.

1. 대상 사용자 / Who should start
2. 첫 검증 명령 / First validation command
3. 다음 문서 / Next doc

검증기가 실패하면 이 묶음 변경은 커밋/푸시하지 않습니다. English mirror: If the validator fails, hold the README bundle change instead of commit/push.
