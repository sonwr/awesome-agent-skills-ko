# README intro-first validate start signal

README 소개 바로 아래에서 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 10초 안에 읽히는지 짧게 다시 확인하는 메모입니다.

English mirror:
A quick note for re-checking that **audience -> first validation command -> next doc** still reads clearly within 10 seconds directly under the README intro.

## 한국어 기준 / Korean-first check

- 소개 바로 아래에 검증형 시작점이 남아 있는지 확인합니다.
- `python3 templates/scripts/validate_template.py`가 첫 명령으로 바로 보이는지 확인합니다.
- `examples/quickstart.md` 또는 다음 문서 handoff가 같은 시야에 남아 있는지 확인합니다.

## English mirror

- Keep the validate-first starter cue directly under the intro.
- Make sure `python3 templates/scripts/validate_template.py` remains the first visible command.
- Keep `examples/quickstart.md` or the next-doc handoff in the same first-screen view.
