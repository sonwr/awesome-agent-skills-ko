# README intro-first validate next-doc progress note

- 한국어: 소개 바로 아래에서 `대상 사용자 -> 첫 검증 명령 -> 다음 문서`가 한 시야에 남아야 바로 진행할 수 있습니다.
- English: Keep `audience cue -> first validation command -> next doc` visible in one first-screen block so newcomers can move immediately.
