# README intro-first 검증 화면 유지 / README intro-first validate keep screen

소개 바로 아래에서 **대상 사용자 -> 첫 검증 명령 -> 다음 문서** 흐름이 같은 첫 화면에 남는지 확인합니다.

- 첫 검증 명령은 `python3 templates/scripts/validate_template.py`
- 다음 문서는 `examples/quickstart.md`
- validator 실패 시 README intro-first 변경은 커밋/푸시하지 않습니다.

English mirror:

Keep **audience -> first validation command -> next doc** visible in the same first-screen block directly under the intro.
