# README_INTRO_FIRST_VALIDATE_KEEP_SCREEN

소개 아래 첫 화면에서 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 같은 시야에 남는지 먼저 확인합니다.

English mirror: Verify that **audience -> first validation command -> next doc** stays visible in the same first-screen view directly under the intro.
