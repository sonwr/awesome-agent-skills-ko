# README_INTRO_FIRST_VALIDATION_TWO_STEP_NOTE

한국어 기본 intro-first README는 첫 화면에서 바로 `python3 templates/scripts/validate_template.py` 검증 명령과 다음 문서 한 개를 함께 보여줘야 합니다. English mirror: keep the first screen anchored to one validation command and one next document so the landing flow stays obvious.
