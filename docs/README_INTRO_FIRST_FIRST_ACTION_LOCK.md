# README intro-first first-action lock

README 상단 소개 바로 아래에서는 **대상 사용자 -> 첫 검증 명령 -> 다음 문서** 순서가 끊기지 않아야 합니다.

English mirror: Directly under the opening intro, keep **audience -> first validation command -> next doc** as one uninterrupted handoff.
