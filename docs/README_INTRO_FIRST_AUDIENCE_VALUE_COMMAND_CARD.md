# README intro-first audience + value + command card

소개 바로 아래에서 **대상 사용자 -> 제공 가치 -> 첫 검증 명령**이 한 카드처럼 읽혀야 첫 화면 intro-first 약속이 유지됩니다.

English mirror: Keep **audience -> immediate value -> first validation command** readable like one card directly under the intro so the landing section still answers who should start and what to do next.
