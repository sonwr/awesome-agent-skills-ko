# README intro-first 검증 경로 약속 / README intro-first validation path promise

소개 바로 아래에서는 **대상 사용자 → 첫 검증 명령 → 다음 문서**가 한 묶음으로 남아야 합니다.

- 검증 명령은 `python3 templates/scripts/validate_template.py`로 고정합니다.
- 다음 문서는 `examples/quickstart.md`와 `docs/BILINGUAL_CONTRIBUTION_CHECKLIST.md`로 바로 이어져야 합니다.
- validator가 실패하면 intro-first 변경은 커밋/푸시하지 않고 상태만 짧게 공유합니다.
