# README intro-first 검증 시작 다음 문서 묶음 / README intro-first validate-start next-doc bundle

README 첫 화면에서는 아래 3단 묶음이 소개 바로 아래에 남아야 합니다.

1. 대상 사용자 / Who should start
2. 첫 검증 명령 / First validation command
3. 다음 문서 / Next doc

짧게 줄여도 `python3 templates/scripts/validate_template.py` 다음에 `examples/quickstart.md`가 바로 보여야 합니다.
English mirror: Even after compressing the landing copy, keep `python3 templates/scripts/validate_template.py` immediately followed by `examples/quickstart.md`.
