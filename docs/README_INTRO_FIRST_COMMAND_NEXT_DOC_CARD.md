# README 소개 바로 아래 첫 명령-다음 문서 카드 / README intro-first command-next-doc card

## 한국어 기준 / Korean-first cue
- 소개 문장 바로 아래에는 첫 검증 명령과 다음 문서가 함께 남아 있어야 합니다.
- 순서는 `대상 사용자 -> 첫 검증 명령 -> 다음 문서`를 유지합니다.
- 첫 명령은 `python3 templates/scripts/validate_template.py`, 다음 문서는 `examples/quickstart.md`입니다.

## English mirror
- Keep the first validation command and next doc directly under the intro.
- Preserve the order `audience -> first validation command -> next doc`.
- The first command is `python3 templates/scripts/validate_template.py`, and the next doc is `examples/quickstart.md`.
