# README 프로젝트 소개-빠른시작 카드 / README project intro-to-quickstart card

## 한국어 기준 / Korean-first card
- README 첫 화면은 프로젝트 소개 → 대상 사용자 → 제공 가치 → 대표 카테고리/예시 → 빠른 시작 순서를 먼저 보여준다.
- 첫 실행 문장은 `python3 templates/scripts/validate_template.py`와 `examples/quickstart.md`를 한 묶음으로 안내한다.
- 기여 규칙·운영 정책은 상단 가치 설명을 가리지 않도록 `CONTRIBUTING.md`, `docs/README_FAST_PATHS.md`, `docs/CURATION_POLICY.md`로 내려 보낸다.

## English mirror
- Keep the README landing order as project intro -> audience -> value -> featured categories/examples -> quick start.
- Pair the first runnable command (`python3 templates/scripts/validate_template.py`) with the next doc (`examples/quickstart.md`) so first-time visitors do not stall.
- Move governance details into `CONTRIBUTING.md`, `docs/README_FAST_PATHS.md`, and `docs/CURATION_POLICY.md` so repo value stays visible before policy detail.

## 유지 규칙 / Maintenance rule
- README 상단을 손볼 때 이 카드의 순서가 유지되는지 먼저 확인한다.
- 첫 화면에서 소개보다 운영 문서 링크가 더 먼저 눈에 띄면 재배치한다.
