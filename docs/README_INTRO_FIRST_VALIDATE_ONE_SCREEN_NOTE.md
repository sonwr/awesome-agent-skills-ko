# README intro-first validate one-screen note

README 첫 화면에서는 소개 바로 아래에 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 같은 시야에 남아야 합니다. English mirror: Keep **audience -> first validation command -> next doc** in the same first-screen view directly under the intro.
