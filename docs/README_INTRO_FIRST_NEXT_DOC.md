# README intro-first next-doc cue

README 상단을 다듬을 때는 소개 바로 아래에 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 한 덩어리로 남아 있는지 먼저 확인합니다.

English mirror:

When tightening the README top section, verify that **audience -> first validation command -> next doc** still stays bundled directly under the intro.
