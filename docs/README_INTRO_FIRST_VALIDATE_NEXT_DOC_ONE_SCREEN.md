# README intro-first validate next doc one-screen

소개 바로 아래 한 화면 안에서 **대상 사용자 -> 첫 검증 명령 -> 다음 문서**가 함께 보여야 합니다.

- 대상 사용자가 첫 화면에서 바로 보인다.
- `python3 templates/scripts/validate_template.py`가 소개 아래에 남아 있다.
- 다음 문서 handoff가 검증 명령 바로 뒤에 붙어 있다.

English mirror: Keep the audience cue, validation command, and next-doc handoff visible together in one first-screen block directly under the intro.
