# README intro-first validate + next-doc one-screen

소개 바로 아래에서 **대상 사용자 → 첫 검증 명령 → 다음 문서**가 같은 시야에 남는지 먼저 확인합니다.

English mirror: Verify that **audience -> first validation command -> next doc** still stays visible in one screen directly under the intro.
